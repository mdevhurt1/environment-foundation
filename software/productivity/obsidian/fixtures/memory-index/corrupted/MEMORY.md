# Memory index

- [Alpha fact](reference_alpha.md) — the nav2 controller DOES declareOn Jazzy the
  parameter, so the remap mechanism is superseded.
- [Beta fact](reference_beta.md) — a health grep over the spine cannot seeheck a
  value that was never marked, so a row shippeds publicly stays unchecked.
