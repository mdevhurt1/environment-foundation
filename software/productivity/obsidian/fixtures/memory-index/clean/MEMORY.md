# Memory index

- [Alpha fact](reference_alpha.md) — the nav2 controller DOES declare the
  parameter, so the remap mechanism is superseded.
- [Beta fact](reference_beta.md) — a health grep over the spine cannot see a
  value that was never marked, so a row shipped publicly stays unchecked.
